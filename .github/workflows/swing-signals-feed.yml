name: Swing Signals Feed

# Generates swing-signals.json — the LIVE feed for the ISOLATED demo swing cBot
# (separate from the intraday signals.json). Runs the validated S5+RSI and
# H&S+macro detectors on the latest historical-ohlc.json and publishes the fresh,
# actionable signals for the swing cBot to MARKET-execute on a demo account.
#
# Every 10 min (offset minutes to dodge the :00 herd) PLUS an external-pinger
# entry point. Although swing setups reference 4H/daily context, the trigger bars
# are H1 — so the feed must publish within minutes of an H1 close, or the cBot
# fills stale (see the stop-invalidation / entry-drift guards in swing_signals.py
# and VikingSwingBridge.cs). GitHub's internal cron throttles to 1-3 h gaps, which
# is exactly what produced the 8-9 h stale fills; the repository_dispatch hook lets
# cron-job.org drive it on the real data-refresh cadence, bypassing the throttle.
# This feed is traded ONLY by the separate swing cBot; the intraday cBot and the
# dashboard never read it.

on:
  schedule:
    # Offset off the :00/:10 herd (minute :07) — a 10-min floor if the pinger is down.
    - cron: '7,17,27,37,47,57 * * * *'
  workflow_dispatch:
  # External-scheduler entry point (same mechanism as fetch-data). cron-job.org POSTs to
  #   https://api.github.com/repos/ModernViking1/vikinginvest-prices/dispatches
  # with body  {"event_type": "swing-feed"}  and an
  #   Authorization: Bearer <fine-grained-PAT>  header (repo Contents: Read & write).
  # Best driven right after each fetch-data publish so a fresh H1 bar -> fresh data ->
  # fresh swing signal all land within a few minutes.
  repository_dispatch:
    types: [swing-feed]

concurrency:
  group: swing-signals-feed
  cancel-in-progress: false

jobs:
  feed:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Guard — required files present
        run: |
          for f in swing_signals.py unified_shadow_harness.py historical-ohlc.json \
                   detect_triggers.py backtest_rsi_per_class.py freshen_h1_tail.py \
                   freshen_daily_tail.py freshen_m15_tail.py \
                   fetch_historical_ohlc.py hs_swing_research.py five_strategies_research.py; do
            if [ ! -f "$f" ]; then echo "::error::$f missing from repo root"; exit 1; fi
          done

      # The 6-hourly full backfill (publish-historical-ohlc.yml) leaves the H1 bars the swing
      # detectors trigger on up to 6 h stale, so the swing cBot's entry-age guard rejects the
      # signals and nothing fills. Splice just the last few days of H1 onto the tail here so
      # THIS run reads current bars. Fail-open: on any error the existing (stale) file is used.
      # The freshened file is DISCARDED below and never committed — the 6-hourly job stays the
      # committed source of truth, so this adds no repo bloat (the file is ~60 MB).
      - name: Freshen H1 tail (swing signals must be current, not up to 6 h stale)
        env:
          OANDA_TOKEN: ${{ secrets.OANDA_TOKEN }}
        run: |
          python freshen_h1_tail.py --days 3 --skip-if-fresh-min 50 \
            || echo "freshen failed (fail-open) — swing feed runs on existing data"

      # cam_rev (Camarilla pivots) + any daily-levels strategy need a CURRENT daily bar: the
      # levels for today are built from yesterday's daily close, and the 6-hourly backfill
      # leaves the daily layer 1-2 days stale — which silently blocked ALL cam_rev emission
      # (0 signals reached the cBot despite fresh m15). Splice the last few days of DAILY
      # bars onto the tail here. Fail-open; discarded with the H1 freshening below.
      - name: Freshen daily tail (cam_rev Camarilla levels need current daily bars)
        env:
          OANDA_TOKEN: ${{ secrets.OANDA_TOKEN }}
        run: |
          python freshen_daily_tail.py --days 10 \
            || echo "daily freshen failed (fail-open) — swing feed runs on existing data"

      # The m15-triggered swing strategies — cam_rev's rejection candle and the 24/7 crypto
      # detectors (twob / engulf_manip / mmove on btc/eth/xrp/sol) — fire on the m15 bar
      # itself, but the H1/daily fresheners above leave m15 to the 6-hourly backfill. So
      # crypto m15 goes 0-6 h stale and every crypto signal is BORN past the cBot's 120-min
      # age guard -> no crypto fills (confirmed 2026-09-19: crypto m15 lagged 3 h, all btc/xrp
      # signals 8-16 h old, cap rejected them). Splice the last couple of days of m15 onto the
      # tail so THIS run reads current bars. Weekends: FX/comm/index just return nothing (closed)
      # and are left as-is; only 24/7 crypto actually freshens. Fail-open; discarded below.
      - name: Freshen m15 tail (m15 strategies — esp. 24/7 crypto — must not be born stale)
        env:
          OANDA_TOKEN: ${{ secrets.OANDA_TOKEN }}
        run: |
          python freshen_m15_tail.py --days 2 --skip-if-fresh-min 12 \
            || echo "m15 freshen failed (fail-open) — swing feed runs on existing data"

      - name: Generate swing feed
        run: python swing_signals.py

      # Drop the transient tail freshening (H1 + daily + m15 all splice into this one file) so
      # the working tree is clean: the commit step stages ONLY swing-signals.json, and a dirty
      # 60 MB historical-ohlc.json would otherwise block the rebase-on-race below (git rebase
      # refuses with unstaged changes).
      - name: Discard transient tail freshening (6-hourly backfill owns this file)
        run: git checkout -- historical-ohlc.json 2>/dev/null || true

      - name: Commit + push swing-signals.json (retry on race)
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- swing-signals.json; then
            echo "no change to swing-signals.json"; exit 0
          fi
          git add swing-signals.json
          git commit -m "chore: update swing signals feed [skip ci]"
          pushed=0
          for i in 1 2 3 4 5; do
            if git push origin main 2>&1; then echo "pushed"; pushed=1; break; fi
            echo "push race, rebasing (attempt $i)"; git fetch origin main && git rebase origin/main || git rebase --abort
            sleep $((2 ** i))
          done
          if [ "$pushed" != "1" ]; then echo "::error::failed to push swing-signals.json after retries"; exit 1; fi

      - name: Purge CDN (so the swing cBot polls fresh signals, not the edge cache)
        run: |
          curl -s -m 10 "https://purge.jsdelivr.net/gh/ModernViking1/vikinginvest-prices@main/swing-signals.json" || echo "purge gh failed"
          curl -s -m 10 "https://purge.jsdelivr.net/npm/vikinginvest-prices@latest/swing-signals.json" || echo "purge npm failed"
